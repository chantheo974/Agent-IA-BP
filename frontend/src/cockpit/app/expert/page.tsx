import Link from 'next/link';
import { PageHeading, Surface } from '@/components/cockpit/page-elements';
import { sheetGroups, sheetHref, sheetViews } from '@/lib/catalog';
export default function ExpertPage(){return <div className="spatial-page space-y-7"><PageHeading eyebrow="Mode expert" title="Les feuilles derrière vos décisions." description="33 vues métier identifiées. Ouvrez une feuille, sa grille détaillée ou ses liens. Les résultats sont protégés et les exemples restent explicitement fictifs."/><div className="grid gap-5 lg:grid-cols-2">{sheetGroups.map((group,index)=><Surface key={group} className="expert-group-card" variant="floating"><h2 className="text-xl font-semibold">{group}</h2><ul className="mt-4 space-y-3">{sheetViews.filter(s=>s.group===index).map(sheet=><li key={sheet.id}><Link href={sheetHref(sheet.id)} className="block rounded-2xl border bg-white p-4 hover:bg-[var(--brand-soft)]"><strong>{sheet.title}</strong><span className="block text-slate-600">{sheet.excel} · {sheet.editable?'Paramètres métier autorisés':'Résultats ou documentation protégés'}</span></Link></li>)}</ul></Surface>)}</div></div>;}
